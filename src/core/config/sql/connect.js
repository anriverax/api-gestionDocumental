export const crdu = {
	config:
		"HOLA" /*
	read: async (query) => {
		try {
			let result;
			await sql.connect(this.config);
			result = await sql.request().query(query);
			console.log(result);
		} catch (error) {
			console.log(error);
		}
	},*/,
};
